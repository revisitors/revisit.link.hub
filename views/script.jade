extend layout

block content
  section
    p.path= services.join(' => ')

    h3
      a(href='/#{token}/play', class='button replay') &#8667;
        span Replay!

    if result
      div.result
        img(src='#{result}')
