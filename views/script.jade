extend layout

block content
  h2 Script for #{token}

  section
    ol
      for service in services
        li
          span= service

    if result
      div.result
        img(src='#{result}')

    h3
      a(href='/#{token}/play', class='button') &#8667;
        span Play!
